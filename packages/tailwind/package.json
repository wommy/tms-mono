{
  "name": "@technomad/tailwind",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "echo testing eleventy with $npm_package_version",
    "build": "postcss src --base -d style --verbose",
    "minify": "esbuild style/*.css --outdir=style/min --out-extension:.css=.min.css --minify",
    "start": "postcss src --base -d style --verbose --watch"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.0",
    "autoprefixer": "^10.4.1",
    "esbuild": "^0.14.11",
    "postcss": "^8.4.5",
    "postcss-cli": "^9.1.0",
    "tailwindcss": "^3.0.11"
  }
}
